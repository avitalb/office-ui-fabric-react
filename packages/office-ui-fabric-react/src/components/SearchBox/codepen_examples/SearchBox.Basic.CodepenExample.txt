let {
  SearchBox,
  Fabric
} = window.Fabric;

const SearchBoxBasicExample = () => (
  <div>
      <SearchBox
        placeholder="Search"
      />
  </div>
);

ReactDOM.render(<SearchBoxBasicExample/>,document.getElementById("content"));