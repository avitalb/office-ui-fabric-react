let {
  Toggle,
  Fabric
} = window.Fabric;

const ToggleBasicExample = () => (
  <div>
    <Toggle
      defaultChecked={false}
      label="Enabled and unchecked"
      onText="On"
      offText="Off"
    />
  </div>
);

ReactDOM.render(<ToggleBasicExample/>,document.getElementById("content"));